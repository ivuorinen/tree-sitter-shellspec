# EditorConfig

Follow `.editorconfig` in every file you write: 2-space indentation, LF line endings, UTF-8,
a final newline, no trailing whitespace outside Markdown, and at most 160 characters per line.
Treat every EditorConfig violation as a blocking error.
